# Unified site Workers delivery acceptance

Recorded 2026-09-23. These are verified deployment snapshots, not floating latest-version claims. Check the live `/build-info.json` and current Cloudflare deployment before operating the site. `https://inquirystack.philohub.workers.dev/` is the approved canonical URL.

## Former GitHub Pages retirement, 2026-09-23

After reconfirming the Worker site's 26 HTTP-readable output hashes and seven principal routes, the four former framework GitHub Pages sites were retired. Each had published from `main/docs`, without a custom domain; the original settings were saved in private deployment state. GitHub then reported `has_pages: false` for AHICP, PPF, Vault Interface, and Starter, and their former page URLs returned HTTP 404. Their repositories and source files were not deleted. All four repository homepage fields continue to point at their canonical Worker sections. The verified prior Worker version remains the immediate rollback point; restoring Pages would require a separate deliberate reconfiguration.

AHICP [PR #43](https://github.com/ChongLiuPhil/AI-Assisted-Human-Inquiry-and-Creation-Protocol/pull/43) replaced the dead links in the bilingual public human guide. PPF [PR #38](https://github.com/ChongLiuPhil/Personal-Publishing-Framework/pull/38) and Vault Interface [PR #15](https://github.com/ChongLiuPhil/Vault-interface/pull/15) corrected their public status; PPF also moved a schema identifier from the retired Pages URL to an HTTP-200 GitHub raw source URL. Starter refresh first exposed a missing Python dependency in its workflow; [PR #42](https://github.com/ChongLiuPhil/Inquiry-Publishing-Project-Starter/pull/42) fixed it. A successful rerun created and merged source-lock [PR #43](https://github.com/ChongLiuPhil/Inquiry-Publishing-Project-Starter/pull/43), pinning all three new upstream revisions. The native Cloudflare build of Starter `a9d10ef4c5ffd8f15be7bb07624cc17bbaf93a88` succeeded and its Worker version received 100% traffic. Live `/build-info.json` pinned AHICP `2b6bbdeb9ac6af0e09bb704c54314afb8d0ec4c3`, PPF `585a42e29b5230ba38e6ee1faecdb9f7f488275a`, and Vault Interface `b896ce373fe6345e0b8ad978d6fec5ce0b24b981`. All 26 HTTP-readable output hashes, seven principal routes, canonical links, robots policy and 404 handling passed again. Both language versions of the guide and all four llms entries contained no retired Pages host.

## Verified canonical cutover, 2026-09-23

The owner approved the free `workers.dev` address for the human `/` and machine `/agent/` entries. AHICP [PR #42](https://github.com/ChongLiuPhil/AI-Assisted-Human-Inquiry-and-Creation-Protocol/pull/42), PPF [PR #37](https://github.com/ChongLiuPhil/Personal-Publishing-Framework/pull/37), and Vault Interface [PR #14](https://github.com/ChongLiuPhil/Vault-interface/pull/14) updated their public entries. Starter source-lock [PR #37](https://github.com/ChongLiuPhil/Inquiry-Publishing-Project-Starter/pull/37) pinned those exact revisions; cutover [PR #38](https://github.com/ChongLiuPhil/Inquiry-Publishing-Project-Starter/pull/38) passed three checks and merged. Its first clean Cloudflare build failed because PyYAML was absent; the previous verified deployment stayed live. Fix [PR #39](https://github.com/ChongLiuPhil/Inquiry-Publishing-Project-Starter/pull/39) added pinned Python dependency installation to both the repository contract and actual provider build command, passed all three checks, and deployed successfully.

Live `/build-info.json` after the successful native build reported Starter `ccf8e27cf578b6fa82d6af3c41c4f8cc1a51124f`, AHICP `6d314c5d81d296b2e8a6dff7d4feea6aaf63b9de`, PPF `52ceb00b45efc86f7d1f635beb2e0da7201ed588`, and Vault Interface `c6f4991556aa280f712ec43a40df39b916da9ba4`; the source-lock SHA256 was `aea40b131c7678a9f6db67b5b7b1ff930244d5d4c8ffe82ad9257795fd1bd8d3`. The build record reported a clean tree, `state: canonical-workers-dev`, and the selected public URL. The new Worker version served 100% of traffic. All 26 HTTP-readable output hashes matched the build record; `_headers` is provider-consumed. `/`, `/agent/`, `/start/`, all four component routes, machine descriptor, canonical tags, robots allowance, absence of `noindex`, and 404 handling passed live checks. Local browser checks on the reviewed cutover PR covered 24 route/viewport/JavaScript combinations.

The prior verified deployment pinned Starter `cd0ce5e5bbcfc64e34c13770929677daff69272a` and remains the immediate rollback version in private deployment state. Re-read the active deployment before any rollback. At that cutover snapshot, the four old GitHub Pages entries returned HTTP 200; the current Cloudflare account listed no Pages projects, so Pages holding is not a verified recovery point. No DNS or paid-plan changes were made. Preview URLs and non-main builds remain disabled; restricted preview and Access reader checks are not accepted.

## Earlier candidate evidence

Native Workers Builds connects only Starter `main`; the root is `/` and the output is `_site`. A PPF commit `89854ad29196f2f70e01e246d3839e3daa4ea159` triggered [Starter refresh 35859708040](https://github.com/ChongLiuPhil/Inquiry-Publishing-Project-Starter/actions/runs/35859708040), App-created [PR #35](https://github.com/ChongLiuPhil/Inquiry-Publishing-Project-Starter/pull/35), ordinary CI, merge as Starter `cc5093b048aadd776c141d86db021109dec09b0d`, and a successful native Cloudflare build and deployment. The live [build record](https://inquirystack.philohub.workers.dev/build-info.json) matched Starter and PPF. It pinned AHICP `032407eccbb70691058bdcb5a73d326aa73dae84` and Vault Interface `599cf997d46ef5f0ca21c4f47b326bd46077c671`; source-lock SHA256 was `7fb2481b21ecbb2e6e71c1f6f0cd0d3005129a5ae050f34ef23520debef8db79`.

All 26 publicly served outputs matched the build record's SHA256 values; `_headers` was checked as response headers because the provider consumes that file. The homepage rendered in a browser and `/agent/` returned anonymous HTTP 200. Replaying the same notification produced [refresh 35860034407](https://github.com/ChongLiuPhil/Inquiry-Publishing-Project-Starter/actions/runs/35860034407) without a new source-lock commit, PR, or deployment. Provider Build, Deployment, and Worker Version IDs stay in private deployment state.

At this earlier snapshot, the main Worker was authorized for anonymous reading but canonical URL cutover had not occurred. Preview URLs and non-main builds were disabled; Access-protected preview had **not** passed acceptance. DNS changes, GitHub Pages retirement, Pages holding removal and paid upgrades were not performed; the historical Pages project claim was not independently verified at that time.

For recovery, re-read the active Worker deployment and source revisions. Revert the source-lock PR through normal checks or restore the previous verified Worker version from private deployment state, then check the served response. The prior candidate used Starter `c10798832cdf7b47990d36a888bbe93b455f3691`. Do not delete projects, force-push or change canonical URLs as a rollback shortcut. The current Cloudflare account has no listed Pages project; verify any alternate account before treating Pages holding as available.
