# Web agent continuation: GitHub and Cloudflare

This handoff reflects the verified canonical Worker cutover of 2026-09-23. First read the [machine entry](https://inquirystack.philohub.workers.dev/agent/), current Starter `main` `AGENTS.md`, `ecosystem.yaml`, Agent Retrieval Contract, [migration contract](CLOUDFLARE_PUBLIC_DELIVERY_MIGRATION.md), [acceptance record](CLOUDFLARE_DEPLOYMENT_ACCEPTANCE.md), `cloudflare-builds.yaml`, `wrangler.jsonc`, and `site/sources.lock.json`. Then check current GitHub revisions, live `/build-info.json`, and the active Cloudflare deployment. Historical SHAs are evidence, not a claim about future latest state. No originating computer or local attachment is needed.

Four independent repositories compose one public site on `inquirystack.philohub.workers.dev`: `/` is the human entry and `/agent/` the machine entry. Only Starter connects to native Workers Builds, using `main`, root `/`, and `_site`. The full build and public main deployment passed real acceptance. Build commands and toolchain pins are in `cloudflare-builds.yaml`.

The `Inquiry Publishing ChongLiuPhil` App is installed on the four selected repositories. Its maximum installation permissions include Contents read, Actions write, and Pull requests write; runtime installation tokens select only Starter and narrower permissions per action. The private key and webhook secret stay in Cloudflare Worker Secrets; the App ID is a runtime text variable. Never put secrets or reader identities in Git, logs, or chat.

A real PPF commit triggered App notification, a Starter source-lock PR, three ordinary CI checks, merge, a native Cloudflare build, and updated live content. Replaying the notification produced no duplicate deployment. See the [acceptance record](CLOUDFLARE_DEPLOYMENT_ACCEPTANCE.md) and [PR #35](https://github.com/ChongLiuPhil/Inquiry-Publishing-Project-Starter/pull/35) for exact revisions and runs. There is no hourly polling. PPF's generic lifecycle tools in [PR #31](https://github.com/ChongLiuPhil/Personal-Publishing-Framework/pull/31) were merged; their `apply` command invokes configured Wrangler and does not itself reconcile native Builds GitHub connections or triggers.

The owner approved anonymous main reading and canonical use of this `workers.dev` address. The live cutover passed acceptance and is recorded as `verified-cutover`. Preview URLs and non-main builds stay disabled; Access-protected preview and approved-reader checks have not passed. Former GitHub Pages remain available. The current Cloudflare account listed no Pages projects on 2026-09-23; do not rely on Pages holding for rollback. DNS and paid plans are unchanged.

For further work, inspect actual resources first and reconcile only differences. After an upstream change, compare the source lock, three PR checks, Starter merge SHA, Cloudflare build, and live `/build-info.json`. Unchanged output should create no lock commit or deployment. Before enabling previews, approve the audience and verify `preview_worker` Access on a real preview, including anonymous denial, approved identity, and direct assets. Roll back through a normal source-lock revert PR or the prior verified Worker version held in private deployment state, then check the served response. Preserve verified prior sites; do not force-push or delete projects.

Routine configuration, verification, and the selected `workers.dev` cutover are authorized. Return to the owner only for login/MFA, new GitHub App grants, reader approval, another domain or DNS authorization, direct secret entry, or a future identity change; provide exact links and steps. Main-site public reading does not authorize public previews or publication of other projects.
